Getting started with git commands on bash :
 * git add -A .
 * git commit -m "Commit"
 * git remote add origin https://www.github.com/RaoulBoulos/git-test
 * git push -u origin master
 * git clone <URL>
 * git log --oneline
 * git fetch
 * git pull
